// This is your Prisma schema file,
// learn more about it in the docs: https://pris.ly/d/prisma-schema

// Looking for ways to speed up your queries, or scale easily with your serverless or edge functions?
// Try Prisma Accelerate: https://pris.ly/cli/accelerate-init

generator client {
  provider = "prisma-client-js"
  output   = "../src/generated/prisma"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}

model WaterBody {
  id          String   @id @default(cuid())
  name        String   @unique
  latitude    Float
  longitude   Float
  description String?
  createdAt   DateTime @default(now())
  updatedAt   DateTime @updatedAt
  
  // Relation to questionnaires
  questionnaires WaterBodyQuestionnaire[]
  
  @@map("water_bodies")
}

model WaterBodyQuestionnaire {
  id          String   @id @default(cuid())
  waterBodyId String
  waterBody   WaterBody @relation(fields: [waterBodyId], references: [id], onDelete: Cascade)
  
  // Water clarity questions
  waterClarityRating Int // 1-5 scale
  
  // Biodiversity presence questions
  fishPresence       Boolean
  birdPresence       Boolean
  otherWildlife      Boolean
  biodiversityNotes  String?
  
  // Vegetation questions
  vegetationDensity  Int // 1-5 scale
  vegetationTypes    String[] // Array of vegetation types
  
  // Additional notes
  generalNotes       String?
  
  // Location and user info
  userLatitude       Float?
  userLongitude      Float?
  userName           String?
  
  createdAt          DateTime @default(now())
  updatedAt          DateTime @updatedAt
  
  @@map("water_body_questionnaires")
}
